---
description: A workflow to start the development server and preview the project.
---

1. Start the development server:
```bash
npm run dev -- --port 3000 --host 0.0.0.0
```

2. View the preview:
The application will be available at `http://localhost:3000`.
