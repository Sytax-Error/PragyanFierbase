import type { VizPluginMetadata } from '@/core/visualization/types';
import thumbnail from '../images/thumbnail.svg';

export const metadata: VizPluginMetadata = {
  name: 'Bar Chart',
  description: 'A custom visualization plugin for displaying data as a bar chart.',
  thumbnail,
};
